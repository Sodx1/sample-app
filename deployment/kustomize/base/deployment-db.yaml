apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-deployment
  labels:
    app: amalkov-app
spec:
  replicas: 1
  selector:
    matchLabels:
      deployment: postgres-db
  template:
    metadata:
      labels:
        deployment: postgres-db
        app: amalkov-app
    spec:
      containers:
        - name: postgres-container
          image: postgres
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
          resources:
            limits:
              memory: 4Gi
              cpu: "2"
          ports:
            - containerPort: 5432
